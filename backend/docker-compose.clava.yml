services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TOOL: clava
    ports:
      - 8080:4000
    volumes:
      - .:/app
      - /app/node_modules
    command: ["node", "server.js"]